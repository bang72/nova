# NOVA Protocol Migration Contract

Every major upgrade produces a deterministic migration artifact containing old-state root, protocol tuple, migration specification hash, activation epoch, test-vector commitment and expected new-state root. Consensus, crypto, VM and transport may change only while preserving constitutional invariants.
